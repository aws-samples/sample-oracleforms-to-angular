# AWS CDK v2 (Python). Pin the major line; use a recent 2.x at deploy time.
aws-cdk-lib>=2.160.0,<3.0.0
constructs>=10.0.0,<11.0.0
# Synth-time SSM read in storage_stack.py (uncached fallback for the
# CloudFront /api/* origin); soft dependency — synth degrades gracefully
# without it, but the fallback then never fires.
boto3>=1.34.0
